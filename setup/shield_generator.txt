1. mongodb setup

docker run -d \
  --name theship-mongo \
  -p 2021:27017 \
  -e MONGO_INITDB_ROOT_USERNAME=theship \
  -e MONGO_INITDB_ROOT_PASSWORD=theship1234 \
  mongo:4.4


2. auf mongodb verbinden
docker exec -it theship-mongo mongo -u theship -p theship1234 --authenticationDatabase admin

3. user erstellen für theshipdb
use theshipdb
switched to db theshipdb

> db.createUser({
...   user: "theship",
...   pwd: "theship1234",
...   roles: [
...     { role: "readWrite", db: "theshipdb" }
...   ]
... })
Successfully added user: {
	"user" : "theship",
	"roles" : [
		{
			"role" : "readWrite",
			"db" : "theshipdb"
		}
	]
}
>

4. Write collection vacuum-energy with a single document having the format { "data": <Binary data as hex string> }

use theshipdb
db["vacuum-energy"].insertOne({ "data": BinData(0, "01234abcdef") })
